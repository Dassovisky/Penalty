import type { ReactNode } from 'react';
import React from 'react';
import type { Item } from 'vtex.checkout-graphql';
declare type TotalItemsType = 'total' | 'distinct' | 'totalAvailable' | 'distinctAvailable';
interface Props {
    allowManualPrice?: boolean;
    items: ItemWithIndex[];
    loading: boolean;
    userType?: string;
    itemCountMode?: TotalItemsType;
    onQuantityChange: (uniqueId: string, value: number, item?: ItemWithIndex) => void;
    onRemove: (uniqueId: string, item?: ItemWithIndex) => void;
    onSetManualPrice?: (price: number, itemIndex: number) => void;
    lazyRenderHeight?: number;
    lazyRenderOffset?: number;
    children: ReactNode;
}
interface ItemWithIndex extends Item {
    index: number;
}
declare const ProductList: React.NamedExoticComponent<Props>;
export default ProductList;
